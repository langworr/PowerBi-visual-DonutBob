import powerbi from "powerbi-visuals-api";
import { ColorHelper } from "powerbi-visuals-utils-colorutils";
import { DonutBobData } from "./dataInterfaces";
import "../style/donutBob.less";
import { DonutBobSettingsModel } from "./donutBobSettingsModel";
import { DonutBobOnObjectService } from "./onObject/donutBobOnObjectService";
import DataView = powerbi.DataView;
import IVisual = powerbi.extensibility.IVisual;
import ISandboxExtendedColorPalette = powerbi.extensibility.ISandboxExtendedColorPalette;
import IVisualHost = powerbi.extensibility.visual.IVisualHost;
import ILocalizationManager = powerbi.extensibility.ILocalizationManager;
import VisualUpdateOptions = powerbi.extensibility.visual.VisualUpdateOptions;
import VisualConstructorOptions = powerbi.extensibility.visual.VisualConstructorOptions;
import FormattingModel = powerbi.visuals.FormattingModel;
export declare class DonutBob implements IVisual {
    private static DonutSlices;
    private static DonutSlice;
    private static DonutHighlightedSlice;
    private static CenterLabelClass;
    private static LegendTitleSelector;
    private static LegendItemSelector;
    private static LegendIconSelector;
    private events;
    private layout;
    private rootElement;
    private svg;
    private mainGroupElement;
    private mainLabelsElement;
    private slicesElement;
    private legendElement;
    private legendGroup;
    private legendItems;
    private colorPalette;
    private colorHelper;
    private visualHost;
    private localizationManager;
    private selectionManager;
    private formattingSettingsService;
    private subSelectionHelper;
    private renderService;
    private legend;
    private behavior;
    private tooltipServiceWrapper;
    visualOnObjectFormatting: DonutBobOnObjectService;
    data: DonutBobData;
    formattingSettings: DonutBobSettingsModel;
    constructor(options: VisualConstructorOptions);
    static converter(dataView: DataView, settings: DonutBobSettingsModel, colors: ISandboxExtendedColorPalette, colorHelper: ColorHelper, visualHost: IVisualHost, localizationManager: ILocalizationManager): DonutBobData;
    private areValidOptions;
    private applySelectionStateToData;
    update(options: VisualUpdateOptions): void;
    private removeHighlightedSlice;
    private transformAndResizeMainSvgElements;
    private bindBehaviorOptions;
    private renderLegend;
    private applyOnObjectStylesToLegend;
    private updateViewPortAccordingToLegend;
    private clear;
    private applyOnObjectFormatting;
    getFormattingModel(): FormattingModel;
    private customOutlineCallback;
    private getXShift;
    private getYShift;
}
